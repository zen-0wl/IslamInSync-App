import 'package:flutter/material.dart';

class NamesQuotesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("99 Allah's Names and Quotes"),
        centerTitle: true,
      ),
      body: Center(
        child: Text("Names and Quotes Content"),
      ),
    );
  }
}